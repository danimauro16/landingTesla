name: Say hello

on: 
  push:
    branches:
      - master

jobs: 
  hello_world:
    runs-on: ubuntu-latest
    steps: 
      - name: Echo the message
        run: | 
          echo "Hello World!"
      - name: dime la fecha
        run: date
      - run: ls -l